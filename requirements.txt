# macOS app dependencies (keep for reference)
# rumps>=0.4.0
# py2app>=0.28

# Web app dependencies
fastapi==0.109.0
uvicorn[standard]==0.27.0
jinja2==3.1.3
python-multipart==0.0.6
python-dotenv==1.0.0

# PostgreSQL (only needed for production, optional for local dev)
# psycopg2-binary==2.9.9

# Testing
pytest>=7.0.0
